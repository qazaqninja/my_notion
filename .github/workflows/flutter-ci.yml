name: flutter-ci

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

# Cancel an in-flight run when a newer commit on the same PR comes in.
concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  analyze-and-test:
    name: analyze + test
    runs-on: ubuntu-latest
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v4

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Flutter doctor (diagnostics)
        run: flutter doctor -v

      - name: Pub get
        run: flutter pub get

      # flutter_clean_arch_bundle/ is vendored reference material that
      # depends on custom_lint_builder (not in pubspec). The analyzer
      # excludes it via analysis_options.yaml.
      #
      # `--fatal-warnings` gates CI on warning + error severity. The
      # very_good_analysis baseline (M830) surfaces many info-level
      # lints that we triage incrementally — they print but do not
      # block merges. Promote to `--fatal-infos` once the info backlog
      # is cleared.
      - name: Analyze
        run: flutter analyze --fatal-warnings

      - name: Run tests with coverage
        run: flutter test --coverage --reporter expanded

      - name: Enforce coverage threshold (57%)
        # Baseline at M821 was 53.9%. Ratchet history:
        #   50 → 55 (M900) → 56 (M934) → 57 (M956).
        # By M965 actual is 58.02% (just crossed 58 from below).
        # Holding 57 for now — 0.02pp margin above 58 would flap
        # CI on a single regression. Will bump to 58 once actual
        # holds at ≥58.5% across two consecutive measurements.
        # Next stop 60% once presentation-layer widget tests land.
        uses: VeryGoodOpenSource/very_good_coverage@v3
        with:
          path: coverage/lcov.info
          min_coverage: 57

      - name: Upload coverage artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: coverage-lcov
          path: coverage/lcov.info
          if-no-files-found: warn
          retention-days: 14
