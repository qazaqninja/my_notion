# Local-only env values. Copy to .env and fill in.
# .env is gitignored; .env.example is committed.
#
# Test fixture used by test/features/editor/selection_line_ops_test.dart
# to exercise extractStripeIdsFromLinesIn against a canonical Stripe-format
# string. Any value matching ^sk_test_[A-Za-z0-9]+$ works; if unset, the
# test falls back to a low-entropy placeholder.
STRIPE_TEST_EXAMPLE_KEY=
